services:
  postgres:
    image: postgres:16-alpine
    container_name: finance-dashboard-postgres
    restart: unless-stopped

    env_file:
      - .env.docker

    ports:
      - "5433:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U finance_user -d finance_dashboard"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: